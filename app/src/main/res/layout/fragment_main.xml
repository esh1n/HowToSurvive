<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?attr/colorPrimary"
        android:minHeight="?attr/actionBarSize"
        app:collapseIcon="@drawable/ic_arrow_backward"
        app:popupTheme="@style/AppTheme.PopupOverlay" />

    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/fragment_main__nav_host_container"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/fragment_main__bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="start"
        android:background="@drawable/bg_bottom_bar"
        app:itemIconSize="40dp"
        app:itemIconTint="@color/bottom_menu_state"
        app:itemTextColor="@color/bottom_menu_state"
        app:labelVisibilityMode="unlabeled"
        app:menu="@menu/main_bottom_navigation" />

</LinearLayout>
